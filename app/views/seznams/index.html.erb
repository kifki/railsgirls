<div class="container">
<h1>Imena</h1>


  
<% @seznams.each do |seznam| %>
  <div class="posameznik">
    <%= link_to image_tag(seznam.slika, :class => 'profilna_slika'), seznam %>

    <h4><%= seznam.ime %></h4>
    
    <td><%= link_to 'Edit', edit_seznam_path(seznam) %></td>
    <td><%= link_to 'Destroy', seznam, confirm: 'Are you sure?', method: :delete %></td>
  </div>

<% end %>


<div class="posameznik">
<%= link_to image_tag('http://www.4blackberry.net/data/programs/images/65228_5843.png', :class => 'profilna_slika'), new_seznam_path %>
</div>
<div style="clear:both;"></div>
</div>